{
  "name": "web_server",
  "version": "1.0.0",
  "description": "Express Web Server to run child process using python && serve as gateway for a React app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
